{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Applicant list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Namelast</th>
                <th>Namefirst</th>
                <th>Namemiddle</th>
                <th>Ssn</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('applicant_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nameLast }}</td>
                <td>{{ entity.nameFirst }}</td>
                <td>{{ entity.nameMiddle }}</td>
                <td>{{ entity.ssn }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('applicant_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('applicant_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('applicant_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
